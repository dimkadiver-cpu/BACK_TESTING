# Golden test fixtures

This directory contains golden output files for regression testing.

Each golden file represents the expected output of a simulation run on a specific chain+policy combination.
Tests in `tests/integration/` compare actual simulator output against these files.
